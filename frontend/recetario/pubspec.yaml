name: recetario
description: "Sistema de gestión académica gastronómica"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_localizations:
    sdk: flutter
  
  # UI
  cupertino_icons: ^1.0.8
  
  # State Management
  provider: ^6.1.0
  
  # Network
  http: ^1.2.2
  http_parser: ^4.0.2
  
  # Supabase
  supabase_flutter: ^2.10.2
  
  # Storage
  shared_preferences: ^2.3.2
  
  # File Handling
  file_picker: ^8.1.4
  path_provider: ^2.1.5
  file_saver: ^0.2.14  # ← AGREGAR ESTA LÍNEA
  
  # Utils
  intl: ^0.20.2
  flutter_dotenv: ^5.2.1
  url_launcher: ^6.3.1
  
  # ✨ Para Portafolio
  cached_network_image: ^3.4.1
  youtube_player_flutter: ^9.1.1
  flutter_staggered_grid_view: ^0.7.0
  image_picker: ^1.1.2
  
  # 🔔 Firebase SOLO para móvil
  firebase_core: ^3.8.1
  firebase_messaging: ^15.1.5
  flutter_local_notifications: ^18.0.1
  
  # 📱 Responsive
  flutter_screenutil: ^5.9.3
  
  # 🎨 Dialogs profesionales (estilo SweetAlert)
  awesome_dialog: ^3.2.1
  
  # ✅ HTML multiplataforma
  universal_html: ^2.2.4
  
  # 📊 DASHBOARD ANALYTICS - GRÁFICOS PROFESIONALES
  syncfusion_flutter_charts: 31.2.12
  
  # 📄 EXPORTAR PDF - GENERACIÓN DE REPORTES
  pdf: ^3.10.7
  printing: ^5.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.1

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/logo.png
    - assets/logo-cenfotec.png

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/logo-cenfotec.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/logo-cenfotec.png"